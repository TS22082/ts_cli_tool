package descriptions

const MdiShort = "Mongo Db Interface: Uses AI to generate MongoDB queries"

const MdiLong = `Uses AI to generate MongoDB queries. Will prompt you for a DB name and a prompt

Example:

es mdi
`
